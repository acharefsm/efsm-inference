
### HI! :) 

All code from the project found in python folder. Java folder only contains a simplified version of Lucas random EFSM generator where I have removed the sampling part.

## WARNING. I have made the gp code a module to make the repo cleaner and hence the functions using GP expects this otherwise the imports at the top of each file will have to be updated. 

### Files: 
 ## corrAnalysisFunctions.ipynb 
   This file contains all code used for the project. 
   Instructions on how to use it is found in markdown cells above each code cell.
   There is a special cell to change the setup for the main evaluation plot where you can change the table lengths, generations, complexities etc.

   ## generateData.py
   Here I have gathered all functions to generate example input tables and random functions so that you can use them outside the notebook file.
   Again instructions and discriptions of each function is added in the file itself. :) 

   ## getProbableCandidates.py
   This file contains a function wich returns two lists: probable and less probable candidates for function inference based on the decsiontree in the report.
   Again, more info is found in the file itself.

   ## efsm
   This is Germans file

   ## testGuards.ipynb 
   Started but never finished testing guards.
   




  
